# 取組ダッシュボードを「家のPCとスマホで同じ内容」にする（Upstash・無料枠あり）
# https://upstash.com で Redis を作成し、REST API の URL と TOKEN をコピー
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 任意: 保存をパスワードで守る（本番向け）
# サーバー側
WORK_BOARD_WRITE_SECRET=
# ブラウザ側（上と同じ文字列。公開されるので推測されにくい長い文字列にする）
NEXT_PUBLIC_WORK_BOARD_WRITE_KEY=
